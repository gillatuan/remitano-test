import styled from "styled-components"

export const Wrapper = styled.div`
  margin-top: 20px;

  .header-left {
    a {
      align-items: center;
      display: flex;
      text-decoration: blink;
    }
    p {
      color: #000;
      font-size: 20px;
      font-weight: bold;
      margin-bottom: 0;
      margin-left: 10px;
    }
  }

  .content {
    margin-top: 70px;
  }
`
