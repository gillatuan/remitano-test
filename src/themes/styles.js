import styled from "styled-components"

export const Wrapper = styled.div`
  margin-top: 20px;

  .header-left {
    p {
      color: #000;
      font-size: 20px;
      font-weight: bold;
      margin-bottom: 0;
      margin-left: 10px;
    }
  }
`
