import styled from "styled-components"

export const Wrapper = styled.div`
  .movie-input {
    .form-input {
      margin-left: 20px;
      max-width: unset;
      width: calc(100% - 120px);
    }
  }
  .share-movie {
    margin-top: 20px;

    button {
      width: 100%;
    }
  }
`
